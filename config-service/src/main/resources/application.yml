server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file:\\\C:\Users\qodbw\Documents\SpringCloudConfig

#파일 프로토콜을 이용해 불러오고자 하는 파일의 경로를 지정한다
